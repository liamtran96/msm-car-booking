# =============================================================================
# MSM Car Booking - Environment Configuration
# Copy this file to .env and update values for your environment
# =============================================================================

# Application
NODE_ENV=production
BUILD_TARGET=production

# Ports
FRONTEND_PORT=3000
BACKEND_PORT=3001
DB_PORT=5432

# Database (PostgreSQL 18)
DB_USERNAME=postgres
DB_PASSWORD=your-secure-password-change-me
DB_NAME=msm_car_booking
DB_SYNCHRONIZE=false
DB_LOGGING=false

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-change-in-production
JWT_EXPIRES_IN=7d

# API Configuration
API_PREFIX=api/v1
CORS_ORIGIN=https://your-domain.com

# Frontend Build Args
VITE_API_URL=/api/v1
VITE_APP_NAME=MSM Car Booking

# =============================================================================
# VPS Memory Optimization Notes (3GB RAM)
# =============================================================================
# PostgreSQL: ~768MB (limited via Docker)
# NestJS Backend: ~640MB (NODE_OPTIONS=--max-old-space-size=512)
# Frontend/Nginx: ~128MB
# System/Docker: ~500MB
# Buffer: ~200MB for spikes
# Total: ~2.2GB used, ~800MB free
# =============================================================================
